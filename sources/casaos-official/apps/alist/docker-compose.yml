name: alist
services:
  alist:
    image: xhofe/alist:v3.40.0
    ports:
    - protocol: tcp
      published: 5244
      target: 5244
    restart: 'no'
    volumes:
    - source: ${CONTAINER_DATA_ROOT}/data
      target: /opt/alist/data
      type: bind
